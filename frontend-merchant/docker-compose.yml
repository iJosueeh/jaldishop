services:
  frontend-merchant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jaldishop-frontend-merchant
    restart: unless-stopped
    ports:
      - "4200:80"
    environment:
      - NODE_ENV=production
